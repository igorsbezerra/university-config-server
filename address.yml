server:
    port: 0

spring:
    application:
        name: address
    datasource:
        url: jdbc:mysql://localhost:3306/university
        username: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update

eureka:
    client:
        service-url:
          defaultZone: http://localhost:8761/eureka

management:
  tracing:
      sampling:
          probability: 1.0